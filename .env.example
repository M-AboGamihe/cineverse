# TMDB API v3 read access key (see https://www.themoviedb.org/settings/api).
# This project reads the key at compile time via --dart-define (recommended for CI and public repos).
#
# Example:
#   flutter run --dart-define=TMDB_API_KEY=your_key_here
#
TMDB_API_KEY=
